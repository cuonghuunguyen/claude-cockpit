import "./App.css";

/**
 * Phase 1, Plan 01-01: scaffold-only placeholder.
 *
 * The queue-of-cards dashboard UI (response-oriented card hierarchy, grouped
 * timeline, workspace·branch) is built in Plan 01-05. This screen exists only
 * to prove the Tauri + React + Vite scaffold renders.
 */
function App() {
  return (
    <main className="container">
      <h1>Claude Cockpit</h1>
      <p>Scaffold running — the live session queue arrives in later plans.</p>
    </main>
  );
}

export default App;
